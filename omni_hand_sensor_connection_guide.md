# 智元灵巧手与传感器连接配置指南

本文档旨在为开发人员和自动化配置 Agent 提供关于 JQ Tools Factory 项目中智元灵巧手（Omni Hand）与触觉传感器连接方式的技术说明。本文档详细描述了串口通信协议、连接逻辑、数据流架构以及相关的控制指令，以确保系统能够正确配置和协同工作。

## 1. 系统架构概述

JQ Tools Factory 采用双串口独立通信架构，通过 Web Serial API 在浏览器端直接与硬件设备进行交互。系统将设备分为两类角色（Role）：

- **检测设备 (Force Role)**：负责施加压力或执行动作，包括 CL2-500N-MH01 压力计和智元灵巧手。
- **传感器产品 (Sensor Role)**：负责采集压力数据，包括 16×16 织物触觉传感器和 32×32 高密度传感器。

这两种角色在系统中独立运行，拥有各自的缓冲区、解析逻辑和状态管理机制。

## 2. 智元灵巧手 (Omni Hand) 连接配置

智元灵巧手作为“检测设备”的一种模式（`robot` 模式）接入系统。

### 2.1 串口通信参数

| 参数 | 设定值 | 说明 |
|------|--------|------|
| 默认波特率 | 460800 | 智元灵巧手标准通信速率 |
| 备选波特率 | 115200, 921600 | 根据具体硬件版本可能有所不同 |
| 数据位 | 8 | 标准配置 |
| 停止位 | 1 | 标准配置 |
| 校验位 | 无 | 标准配置 |

### 2.2 通信协议规范

灵巧手采用基于 CRC16-CCITT 校验的二进制指令协议。

**帧结构定义：**
- **帧头**：`0xEE 0xAA` (2字节)
- **设备ID**：2字节 (Little-Endian，默认值为 `1`)
- **数据长度**：1字节 (包含命令字和数据段的长度)
- **命令字**：1字节
- **数据段**：N字节
- **校验和**：2字节 (CRC16-CCITT，Little-Endian)

**核心控制指令：**

1. **使能指令 (Enable)**
   - 命令字：`0x01`
   - 数据段：`[0x01]`
   - 作用：激活灵巧手电机，准备接收位置控制指令。系统在连接成功后会自动发送此指令。

2. **失能指令 (Disable)**
   - 命令字：`0x01`
   - 数据段：`[0x00]`
   - 作用：释放灵巧手电机扭矩。系统在断开连接前会自动发送此指令。

3. **位置控制指令 (Set Positions)**
   - 命令字：`0x08`
   - 数据段：20字节（10个轴的位置数据，每个轴2字节，Little-Endian）
   - 作用：控制灵巧手各关节运动到指定位置。位置值范围为 `0` 到 `4096`。

### 2.3 连接与控制逻辑

在 `Home.tsx` 中，灵巧手的连接逻辑如下：

1. 用户在 `SerialConnectPanel` 中选择“机械手”并指定波特率。
2. 调用 `useSerialPort` 的 `connect` 方法，传入 `deviceMode = 'robot'`。
3. 在 `robot` 模式下，系统会跳过压力计的初始化指令。
4. 串口打开成功后，系统立即构建并发送使能指令（`buildEnablePacket`）。
5. 在耐久性测试页面（`DurabilityPage.tsx`）中，用户可以通过解析 JSON 动作文件，将位置控制指令（`buildSetPositionsPacket`）发送给灵巧手，实现循环抓握测试。

## 3. 触觉传感器连接配置

传感器产品作为独立的数据源接入系统，支持两种不同的硬件协议。

### 3.1 16×16 织物触觉传感器 (双包协议)

| 参数 | 设定值 | 说明 |
|------|--------|------|
| 默认波特率 | 921600 | 高速数据传输 |
| 矩阵尺寸 | 16×16 | 256个采样点 |
| 帧头 | `AA 55 03 99` | 4字节固定分隔符 |

**双包协议结构：**
由于数据量较大，一帧完整数据被拆分为两个数据包发送：
- **PKT01**：帧头 + `0x01`(包号) + 设备类型(1字节) + 128字节数据（第1~128点）
- **PKT02**：帧头 + `0x02`(包号) + 设备类型(1字节) + 144字节数据（第129~256点 + 16字节陀螺仪数据）

**设备类型标识：**
- `0x01`: LH (Left Hand)
- `0x02`: RH (Right Hand)
- `0x03`: LF (Left Foot)
- `0x04`: RF (Right Foot)
- `0x05`: WB (Whole Body)

### 3.2 32×32 高密度传感器 (单帧协议)

| 参数 | 设定值 | 说明 |
|------|--------|------|
| 默认波特率 | 1000000 | 极高速数据传输 |
| 矩阵尺寸 | 32×32 | 1024个采样点 |
| 采样频率 | 100Hz | 硬件固定输出频率 |
| 帧头 | `AA 55 03 99` | 4字节固定分隔符 |

**单帧协议结构：**
- **总帧长**：1028字节
- **数据域**：1024字节（紧跟帧头之后）
- **矩阵映射**：接收到的1024字节数据需要通过特定的映射表（`MATRIX_32X32_MAP`）重排，才能还原为正确的 32×32 物理空间矩阵。

### 3.3 数据流与同步机制

为了处理高频的传感器数据（最高可达 100Hz）而不阻塞浏览器主线程，系统采用了 **SensorDataStreamV2** 架构：

1. **零延迟写入**：串口接收到完整帧后，立即将数据写入全局单例的 Ref 中，不触发 React 状态更新。
2. **UI 节流更新**：`Home.tsx` 中维护一个 100ms 的定时器，定期从 Ref 中读取最新数据并批量更新 UI 状态。
3. **自适应采集**：在进行数据采集时，系统会根据传感器实际的上报频率（通过帧序号去重机制计算得出）进行记录，确保采集频率与硬件真实帧率一致。

## 4. 协同工作流程

在耐久性测试（Durability Test）场景中，灵巧手与传感器需要紧密协同：

1. **初始化**：分别连接灵巧手（460800波特率）和传感器（如 16×16，921600波特率）。
2. **动作编排**：加载 JSON 格式的动作序列（如 `hold`, `ok`, `release`）。
3. **执行与采集**：
   - 系统向灵巧手发送位置控制指令，驱动其执行抓握动作。
   - 同时，传感器持续高频上报受力数据。
   - 数据采集模块根据用户选择的采样点，实时记录 ADC 数值变化。
4. **结束清理**：测试完成后，系统发送失能指令释放灵巧手，并停止数据采集。

## 5. 配置 Agent 注意事项

当自动化 Agent 进行系统配置或二次开发时，请注意以下关键点：

- **波特率匹配**：必须确保所选设备的波特率与硬件实际配置完全一致，否则会导致数据解析失败或乱码。
- **指令时序**：在发送灵巧手控制指令时，应确保设备已成功连接并处于使能状态。连续发送多条指令时，建议加入适当的延时（如 200ms），以防硬件缓冲区溢出。
- **数据去重**：传感器数据采集中已内置帧序号去重机制，配置采集逻辑时无需额外处理重复帧问题，直接读取 `SensorDataStreamV2` 的最新快照即可。
- **UI 性能**：严禁在串口 `onData` 回调中直接调用 `setState`，必须遵循现有的 Ref 暂存 + 定时器批量更新的架构，以维持系统的高性能响应。
